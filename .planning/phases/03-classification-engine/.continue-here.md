---
context: phase
phase: 03-classification-engine
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-05-01T00:22:04Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through actual production data.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Haiku-only mode — Sonnet escalation is intentionally disabled. Do NOT re-enable it without explicit user approval. See decision log below.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Confidence threshold escalation | Using a 0.8 confidence threshold with Haiku caused ~91% of emails to hit both Haiku AND Sonnet, doubling cost on nearly every classification. Haiku's calibration returns 0.6-0.79 even on clear-cut emails. | advisory | Escalation is now removed entirely. If re-adding, use noMatchFound only — never a confidence threshold. |

<current_state>
Phase 3 Wave 5 (plan 03-05) is the only remaining plan. It is a fully manual verification plan — no code to write. Three tasks:
- Task 1: Send 8 test emails to rebekah@trueocean.com, run psql queries A-G, paste results (BLOCKED on Anthropic credits)
- Task 2: 24h later, verify 2FA test email auto-deleted from Gmail
- Task 3: Write 03-05-SUMMARY.md with evidence (auto, runs after Tasks 1+2)

The production system is healthy and running the Haiku-only build (commit b048a8acb).
</current_state>

<completed_work>

This session (2026-04-30 / 2026-05-01):
- Diagnosed API credit burn: ~91% of emails were hitting both Haiku AND Sonnet due to 0.8 confidence threshold
- Removed Sonnet escalation entirely — Haiku-only classification (commit b048a8acb)
- Deployed new build to production: docker compose pull + up -d confirmed healthy

Prior sessions (Waves 1-4, all complete):
- Wave 1 (03-01): SSM env vars set, ECONOMY_LLM_* configured, Anthropic balance verified
- Wave 2 (03-02): Prisma migration added ExecutedRule.confidenceScore, RED test stubs written
- Wave 3 (03-03): 8 canonical rules seeded in production DB, 6 old rules deleted
- Wave 4 (03-04): Two-call escalation wired, confidenceScore threaded end-to-end, meta-rule guard added, deployed (commit 42fdd8ec1)
</completed_work>

<remaining_work>

Plan 03-05 — End-to-End Production Verification (all 3 tasks pending):

Task 1 (human-verify, BLOCKING):
- Pre-flight: top up Anthropic credits to ~$10 at console.anthropic.com
- Set up: SSH log tail + psql shell (see 03-05-PLAN.md for exact commands)
- Send 8 test emails per table in plan (T1 Newsletter, T2 Receipts, T3 Deals, T4 Marketing, T5 Urgent, T6 2FA, T7 Uncertain, T8 greers@trueocean.com)
- Run psql queries A-G, paste all output in chat
- Resume signal: "verified" with all query outputs

Task 2 (human-verify, 24h later):
- Check Gmail Trash for T6 (2FA test email)
- Run ScheduledAction query for T6 row
- Resume signal: "deleted" with ScheduledAction row state

Task 3 (auto):
- Write 03-05-SUMMARY.md with full evidence table
- Mark CLASS-01 through CLASS-08 complete in REQUIREMENTS.md
- Update ROADMAP.md and STATE.md to Phase 4
</remaining_work>

<decisions_made>

- **Haiku-only classification** (2026-05-01): Sonnet escalation removed entirely. Root cause was 0.8 confidence threshold causing 99/109 emails to pay for both models. New policy: Haiku classifies everything; if accuracy proves insufficient, add Sonnet back for specific categories only (not blanket escalation). Commit: b048a8acb.
- **No Ollama/local LLM**: At current EC2 setup, GPU instance costs ($380+/month) far exceed Haiku API costs. Not worth pursuing.
- 8 canonical classification rules are seeded in production and working correctly per Wave 4 deployment.
- confidenceScore is stored in ExecutedRule for monitoring but no longer drives escalation logic.
</decisions_made>

<blockers>
- Anthropic API credits exhausted — classification has been paused for ~24h. Top up before testing.
- Wave 5 verification is fully manual — requires Rebekah to send test emails and inspect DB/Gmail.
</blockers>

## Required Reading (in order)
1. `.planning/phases/03-classification-engine/03-05-PLAN.md` — full Task 1 setup instructions, test email table, psql queries A-G, and acceptance criteria
2. `.planning/phases/03-classification-engine/03-04-SUMMARY.md` — confirms what's in production (commit 42fdd8ec1, confidenceScore column deployed)
3. `.planning/phases/03-classification-engine/03-CONTEXT.md` — D-05 (exact actions per rule), D-11 (2FA delete delay)

## Infrastructure State
- Production app: `inbox-zero-app` running on inbox.tdfurn.com, image `ghcr.io/rebekah-create/inbox-zero-rebekah:latest` (commit b048a8acb — Haiku-only)
- SSH key: `~/.ssh/inbox_key`
- DB access: `docker compose exec -T postgres sh -c "psql \$POSTGRES_DB -U \$POSTGRES_USER -c \"...\""` from `/opt/inbox-zero`
- Log tail: `ssh -i ~/.ssh/inbox_key ubuntu@inbox.tdfurn.com 'cd /opt/inbox-zero && docker compose logs -f --tail=0 app'`
- Anthropic credits: EXHAUSTED — top up at console.anthropic.com before testing

<context>
Session paused because API credits ran out. Used the downtime productively — diagnosed the cost problem from production logs (91% Sonnet escalation rate), fixed it by removing the confidence threshold escalation entirely (Haiku-only), and deployed the fix. Phase 3 verification (03-05) is still pending — it requires manual email testing which can't proceed until credits are replenished. Once credits are topped up, run 03-05 Task 1 directly. The system is otherwise healthy and ready to test.
</context>

<next_action>
1. Top up Anthropic API credits to ~$10 at console.anthropic.com
2. Open 03-05-PLAN.md and follow Task 1 setup instructions (SSH log tail + psql shell)
3. Send 8 test emails per the plan's table, wait 3 minutes, run queries A-G
4. Paste all output in chat and reply "verified"
</next_action>
