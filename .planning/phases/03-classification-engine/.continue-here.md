---
context: phase
phase: 03-classification-engine
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-05-02T06:00:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through actual production data.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Haiku-only mode — Sonnet escalation is intentionally disabled. Do NOT re-enable it without explicit user approval.
- [ ] CONSTRAINT: Conversation tracking rules disabled — TO_REPLY, AWAITING_REPLY, FYI, ACTIONED are all set to `enabled=false` in the DB. This was a deliberate cost fix. Do NOT re-enable without explicit user approval and a cost budget allocation.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Confidence threshold escalation | 0.8 confidence threshold caused ~91% double-billing (Haiku + Sonnet). Haiku calibration returns 0.6-0.79 even on clear matches. | advisory | Escalation removed entirely. If re-adding, use noMatchFound only. |
| Hidden Sonnet calls via modelType="default" | `aiDetermineThreadStatus` and `aiGenerateArgs` both receive `modelType` from the webhook handler, which passes `"default"` = Sonnet. Only `aiChooseRule` was changed to Haiku. Other call sites were not. | advisory | Any future LLM call site added to the email processing pipeline must explicitly pass `"economy"` unless Sonnet quality is specifically justified and budgeted. |

<current_state>
Phase 3 Wave 5 (plan 03-05) is the only remaining plan. It is a fully manual verification plan — no code to write. Three tasks, none started:
- Task 1: Send 8 test emails, run psql queries A-G, paste results (unblocked — credits at ~$4.07, cost now ~$0.004/email)
- Task 2: 24h later, verify 2FA test email auto-deleted from Gmail
- Task 3: Write 03-05-SUMMARY.md with evidence (auto, runs after Tasks 1+2)

Production is running the Haiku-only build (commit b048a8acb) with conversation tracking disabled.

Live Gmail audit (2026-05-02) confirmed classification is working for newsletters/marketing. Two gaps identified for Phase 4: transactional emails and vendor marketing. See HANDOFF.json decisions for details.
</current_state>

<completed_work>

This session (2026-05-02):
- Live email audit via Gmail MCP: reviewed 50 emails from last 24h
- Confirmed classification working: newsletters/marketing routed correctly
- Identified two new rule categories for Phase 4 (transactional emails, vendor marketing)
- Validated Phase 4 daily digest architecture: batch Haiku call at 9am, ~$0.25/month
- Confirmed classification code uses stringifyEmail(email, 500) — full body truncated at 500 chars, not Gmail snippet

Prior sessions (Waves 1-4, all complete):
- Wave 1 (03-01): SSM env vars set, ECONOMY_LLM_* configured
- Wave 2 (03-02): Prisma migration added ExecutedRule.confidenceScore, RED test stubs written
- Wave 3 (03-03): 8 canonical rules seeded in production DB, 6 old rules deleted
- Wave 4 (03-04): Two-call escalation wired, confidenceScore threaded end-to-end, meta-rule guard added, deployed (commit 42fdd8ec1)
- Wave 4.5 (b048a8acb): Sonnet escalation removed entirely — Haiku-only
- Wave 4.6 (DB-only): Conversation tracking rules disabled — saved ~$0.013/person-email
</completed_work>

<remaining_work>

Plan 03-05 — End-to-End Production Verification (all 3 tasks pending):

Task 1 (human-verify, BLOCKING):
- Set up: SSH log tail + psql shell (see 03-05-PLAN.md for exact commands)
- Send 8 test emails per table in plan (T1 Newsletter, T2 Receipts, T3 Deals, T4 Marketing, T5 Urgent, T6 2FA, T7 Uncertain, T8 greers@trueocean.com)
- Run psql queries A-G, paste all output in chat
- Resume signal: paste query results and say "verified"

Task 2 (human-verify, 24h later):
- Check Gmail Trash for T6 (2FA test email)
- Run ScheduledAction query for T6 row
- Resume signal: "deleted" with ScheduledAction row state

Task 3 (auto):
- Write 03-05-SUMMARY.md with full evidence table
- Mark CLASS-01 through CLASS-08 complete in REQUIREMENTS.md
- Update ROADMAP.md and STATE.md to Phase 4
</remaining_work>

<decisions_made>

- **Haiku-only classification** (2026-05-01, b048a8acb): Sonnet escalation removed. Root cause: 0.8 confidence threshold caused 99/109 emails to pay for both models.
- **Conversation tracking disabled** (2026-05-01, DB only): TO_REPLY/AWAITING_REPLY/FYI/ACTIONED disabled. Root cause: aiDetermineThreadStatus used modelType="default"=Sonnet on every person-email. Saved ~$0.013/person-email.
- **Phase 4 daily digest architecture confirmed** (2026-05-02): Batch Haiku call at 9am. Pull last 24h threads via Gmail API. Use full body truncated at 500 chars (same as classification — stringifyEmail). One bullet per email, expanded for important ones. ~$0.25/month. Better quality than per-email because model sees full picture for relative importance and grouping.
- **Two new Phase 4 rule categories** (2026-05-02): (1) Transactional emails — label + archive, keep searchable. (2) Vendor marketing — archive like newsletters. Current classification misses both.
- **Bulk Unsubscribe first** (2026-05-01): Before building new sender-triage UI, try existing /bulk-unsubscribe feature.
- **Prompt caching deferred**: At 85 emails/day, cache TTL (5 min) expires between most emails. Low value.
</decisions_made>

<blockers>
- None currently blocking. Credits at ~$4.07, cost now ~$0.004/email — sufficient for Task 1 verification.
</blockers>

## Required Reading (in order)
1. `.planning/phases/03-classification-engine/03-05-PLAN.md` — full Task 1 setup instructions, test email table, psql queries A-G, and acceptance criteria
2. `.planning/phases/03-classification-engine/03-04-SUMMARY.md` — confirms what's in production (commit 42fdd8ec1, confidenceScore column deployed)

## Infrastructure State
- Production app: `inbox-zero-app` running on inbox.tdfurn.com, image `ghcr.io/rebekah-create/inbox-zero-rebekah:latest` (commit b048a8acb — Haiku-only)
- SSH key: `~/.ssh/inbox_key`
- DB access: `docker compose exec -T postgres sh -c "psql \$POSTGRES_DB -U \$POSTGRES_USER -c \"...\""` from `/opt/inbox-zero`
- Log tail: `ssh -i ~/.ssh/inbox_key ubuntu@inbox.tdfurn.com 'cd /opt/inbox-zero && docker compose logs -f --tail=0 app'`
- Anthropic credits: ~$4.07 (sufficient — cost is now ~$0.004/email after conversation tracking disabled)
- Conversation tracking rules: DISABLED in DB (TO_REPLY, AWAITING_REPLY, FYI, ACTIONED all `enabled=false`)

<context>
Two sessions of cost work behind us. Production stable at ~$0.004/email (~$10/month at current volume). Phase 3 is done except for the manual verification run (03-05). 

This session surfaced a clearer picture of Phase 4: the daily digest is the highest-value next feature (cheap, high signal, replaces needing to read every email), and two new rule categories (transactional, vendor marketing) will complete the classification picture. The architecture is well-understood — just needs planning and execution.

Next session: either knock out Phase 3 verification (fast, just needs 8 test emails + psql paste) or jump into Phase 4 planning since the design is clear.
</context>

<next_action>
Option A (complete Phase 3 first): Open 03-05-PLAN.md, follow Task 1 setup instructions, send 8 test emails, run queries A-G, paste results.
Option B (skip to Phase 4): Plan Phase 4 daily digest — batch Haiku call at 9am, Gmail API pull, stringifyEmail(email, 500) per thread, one bullet per email output format.
</next_action>
