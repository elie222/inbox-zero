VERSION 2

NODE get_emails_by_week
DESCRIPTION >
  Get the number of emails sent by week

SQL >
  %
    SELECT
        toStartOfWeek(fromUnixTimestamp64Milli(timestamp)) AS week_start,
        COUNT(*) as count
    FROM email__v3
    WHERE
        "ownerEmail" = {{ String(ownerEmail) }}
        AND "sent" = 0
        AND "draft" = 0
    GROUP BY week_start
    ORDER BY week_start
