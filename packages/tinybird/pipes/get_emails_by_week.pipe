VERSION 1

NODE get_emails_by_week
SQL >
  %
    SELECT
        toStartOfWeek(fromUnixTimestamp64Milli(timestamp)) AS week_start,
        COUNT(*) as count
    FROM email__v3
    WHERE ownerEmail = {{ String(ownerEmail) }}
    GROUP BY week_start
    ORDER BY week_start
