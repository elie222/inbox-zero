import { describe, it, expect } from "vitest";

describe("generateDigestContent — Sonnet batched call", () => {
  it.skip("calls createGenerateObject with promptHardening level: 'full'", () => {
    expect.fail("implemented in 04-04");
  });
  it.skip("returns object matching digestContentSchema shape", () => {
    expect.fail("implemented in 04-04");
  });
  it.skip("input prompt token count stays ≤ 35K for 50-item fixture", () => {
    expect.fail("implemented in 04-04");
  });
});
